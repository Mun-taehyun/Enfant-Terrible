<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enfantTerrible.enfantTerrible.mapper.admin.product.AdminProductSkuOptionMapper">

  <insert id="insert">
    INSERT INTO et_product_sku_option (
      sku_id,
      option_value_id
    ) VALUES (
      #{skuId},
      #{optionValueId}
    )
  </insert>

  <delete id="deleteBySkuId">
    DELETE FROM et_product_sku_option
    WHERE sku_id = #{skuId}
  </delete>

  <select id="findOptionValueIdsBySkuId" resultType="long">
    SELECT option_value_id
    FROM et_product_sku_option
    WHERE sku_id = #{skuId}
  </select>

</mapper>
