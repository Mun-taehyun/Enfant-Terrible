<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enfantTerrible.enfantTerrible.mapper.admin.product.AdminProductOptionValueMapper">

  <select id="findByGroupId" resultType="AdminProductOptionValueRow">
    SELECT
      option_value_id,
      option_group_id,
      value,
      sort_order
    FROM et_product_option_value
    WHERE option_group_id = #{optionGroupId}
      AND deleted_at IS NULL
    ORDER BY sort_order ASC, option_value_id ASC
  </select>

  <select id="findById" resultType="AdminProductOptionValueRow">
    SELECT
      option_value_id,
      option_group_id,
      value,
      sort_order
    FROM et_product_option_value
    WHERE option_value_id = #{optionValueId}
      AND deleted_at IS NULL
  </select>

  <insert id="insert">
    INSERT INTO et_product_option_value (
      option_group_id,
      value,
      sort_order
    ) VALUES (
      #{optionGroupId},
      #{value},
      #{sortOrder}
    )
  </insert>

  <update id="update">
    UPDATE et_product_option_value
    SET
      value = #{req.value},
      sort_order = #{req.sortOrder}
    WHERE option_value_id = #{optionValueId}
      AND deleted_at IS NULL
  </update>

  <update id="softDelete">
    UPDATE et_product_option_value
    SET deleted_at = NOW()
    WHERE option_value_id = #{optionValueId}
      AND deleted_at IS NULL
  </update>

</mapper>
