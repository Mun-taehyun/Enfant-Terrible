<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enfantTerrible.enfantTerrible.mapper.admin.popup.AdminPopupMapper">

  <sql id="adminPopupWhere">
    WHERE p.deleted_at IS NULL

    <if test="req.title != null and req.title != ''">
      AND p.title LIKE CONCAT('%', #{req.title}, '%')
    </if>

    <if test="req.isActive != null">
      AND p.is_active = #{req.isActive}
    </if>
  </sql>

  <select id="findPopups" resultType="com.enfantTerrible.enfantTerrible.dto.admin.popup.AdminPopupListResponse">
    SELECT
      p.popup_id,
      p.title,
      p.link_url,
      p.position,
      p.is_active,
      p.start_at,
      p.end_at,
      p.created_at
    FROM et_popup p
    <include refid="adminPopupWhere" />
    ORDER BY p.popup_id DESC
    LIMIT #{req.size} OFFSET #{req.offset}
  </select>

  <select id="countPopups" resultType="int">
    SELECT COUNT(*)
    FROM et_popup p
    <include refid="adminPopupWhere" />
  </select>

  <select id="findById" resultType="com.enfantTerrible.enfantTerrible.dto.admin.popup.AdminPopupDetailResponse">
    SELECT
      p.popup_id,
      p.title,
      p.content,
      p.link_url,
      p.position,
      p.width,
      p.height,
      p.is_active,
      p.start_at,
      p.end_at,
      p.created_at,
      p.updated_at,
      p.deleted_at
    FROM et_popup p
    WHERE p.popup_id = #{popupId}
  </select>

  <insert id="insert">
    INSERT INTO et_popup (
      title,
      content,
      link_url,
      position,
      width,
      height,
      is_active,
      start_at,
      end_at
    ) VALUES (
      #{title},
      #{content},
      #{linkUrl},
      #{position},
      #{width},
      #{height},
      COALESCE(#{isActive}, 1),
      #{startAt},
      #{endAt}
    )
  </insert>

  <select id="findLastInsertId" resultType="long">
    SELECT LAST_INSERT_ID()
  </select>

  <update id="update">
    UPDATE et_popup
    SET
      title = #{title},
      content = #{content},
      link_url = #{linkUrl},
      position = #{position},
      width = #{width},
      height = #{height},
      is_active = COALESCE(#{isActive}, 1),
      start_at = #{startAt},
      end_at = #{endAt}
    WHERE popup_id = #{popupId}
      AND deleted_at IS NULL
  </update>

  <update id="softDelete">
    UPDATE et_popup
    SET deleted_at = NOW()
    WHERE popup_id = #{popupId}
      AND deleted_at IS NULL
  </update>

</mapper>
