<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enfantTerrible.enfantTerrible.mapper.admin.inquiry.AdminProductInquiryMapper">

  <sql id="adminInquiryWhere">
    WHERE i.deleted_at IS NULL

    <if test="req.productId != null">
      AND i.product_id = #{req.productId}
    </if>

    <if test="req.userId != null">
      AND i.user_id = #{req.userId}
    </if>

    <if test="req.status != null and req.status != ''">
      AND i.status = #{req.status}
    </if>
  </sql>

  <select id="findInquiries" resultType="com.enfantTerrible.enfantTerrible.dto.admin.inquiry.AdminProductInquiryListResponse">
    SELECT
      i.inquiry_id,
      i.product_id,
      i.user_id,
      u.email AS user_email,
      i.content,
      i.is_private,
      i.status,
      i.answer_content,
      i.answered_by_user_id,
      i.answered_at,
      i.created_at
    FROM et_product_inquiry i
    INNER JOIN et_user u ON u.user_id = i.user_id
    <include refid="adminInquiryWhere" />
    ORDER BY i.inquiry_id DESC
    LIMIT #{req.size} OFFSET #{req.offset}
  </select>

  <select id="countInquiries" resultType="int">
    SELECT COUNT(*)
    FROM et_product_inquiry i
    <include refid="adminInquiryWhere" />
  </select>

</mapper>
