<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.enfantTerrible.enfantTerrible.mapper.admin.product.AdminProductOptionGroupMapper">

  <select id="findByProductId" resultType="AdminProductOptionGroupRow">
    SELECT
      option_group_id,
      product_id,
      name,
      sort_order,
      created_at
    FROM et_product_option_group
    WHERE product_id = #{productId}
      AND deleted_at IS NULL
    ORDER BY sort_order ASC, option_group_id ASC
  </select>

  <select id="findById" resultType="AdminProductOptionGroupRow">
    SELECT
      option_group_id,
      product_id,
      name,
      sort_order,
      created_at
    FROM et_product_option_group
    WHERE option_group_id = #{optionGroupId}
      AND deleted_at IS NULL
  </select>

  <insert id="insert">
    INSERT INTO et_product_option_group (
      product_id,
      name,
      sort_order
    ) VALUES (
      #{productId},
      #{name},
      #{sortOrder}
    )
  </insert>

  <update id="update">
    UPDATE et_product_option_group
    SET
      name = #{req.name},
      sort_order = #{req.sortOrder}
    WHERE option_group_id = #{optionGroupId}
      AND deleted_at IS NULL
  </update>

  <update id="softDelete">
    UPDATE et_product_option_group
    SET deleted_at = NOW()
    WHERE option_group_id = #{optionGroupId}
      AND deleted_at IS NULL
  </update>

</mapper>
